; Generated by Inmanta for {{ domain }}
$TTL {{ ttl }}
@       IN  SOA {{ master.host.name }}. {{ hostmaster | replace('@', '.') }}. (
        __SERIAL__
        {{ refresh }}
        {{ retry }}
        {{ expiry }}
        {{ ttl }}
)